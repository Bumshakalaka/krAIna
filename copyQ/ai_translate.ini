[Command]
Command="
    copyq:
    const result = execute('~/krAIna/kraina.sh', \"translate\",input());
    if (result.exit_code != 0) {
        copy(str(result.stderr).trim());
    } else {
        copy(str(result.stdout).trim());
    }
    hide();
    paste();"
Icon=\xf15b
InMenu=true
Input=text/plain
Name=ai:translate
Tab=&clipboard